import{createClient}from"npm:@supabase/supabase-js@2.112.3";
export async function requireAdmin(req:Request){const token=(req.headers.get("authorization")||"").replace(/^Bearer\s+/i,"");const url=Deno.env.get("SUPABASE_URL");const key=Deno.env.get("SUPABASE_SECRET_KEY");if(!url||!key||!token)return null;const client=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}});const[{data:{user}},{data:aal}]=await Promise.all([client.auth.getUser(token),client.auth.mfa.getAuthenticatorAssuranceLevel(token)]);if(!user||user.app_metadata?.role!=="admin"||aal?.currentLevel!=="aal2")return null;return{client,user}}
