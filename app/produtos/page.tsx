import{requireUser}from"@/lib/auth";import{createClient}from"@/lib/supabase/server";import ProdutosClient from"./produtos-client";export default async function Produtos(){await requireUser("/produtos");const{data}=await(await createClient()).from("user_products").select("product_id,public_code,status,activated_at").order("activated_at",{ascending:false});return <ProdutosClient initialProducts={data??[]}/>}
